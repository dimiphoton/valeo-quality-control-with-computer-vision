__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/
.DS_Store

# Coupe / pause de l'autopilot Cursor (local, ne pas committer)
.cursor/autopilot.off
.cursor/autopilot.pause

# Données du challenge (taille, licence) : rien de data/ dans git
/data/

# MLflow local + checkpoints (trop lourds pour git)
mlflow.db
mlartifacts/
mlruns/
models/*.pt
models/*.pth
models/*.pkl
models/*.onnx
models/*.npz

# Décommente si les modèles deviennent trop volumineux pour git classique
# models/*.pkl
# models/*.pt
