# Copier vers `.env` en local — ne jamais committer `.env`.

# Destinataire des alertes SNS / AWS Budgets (stack billing, us-east-1)
# ALERT_EMAIL=toi@exemple.fr

# Compte AWS (déploiement — exige AWS CLI déjà installé, jamais via ce repo)
# AWS_REGION=eu-west-3
# AWS_PROFILE=valeo-qc
